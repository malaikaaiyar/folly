---
import { getCollection } from "astro:content";
import { generateExcerpt, formatTitle } from "../utils.js";
import CustomTitle from "./CustomTitle.astro";

// Get all published articles
const allArticles = await getCollection("articles", ({ data }) => {
  return data.published == true;
});

// Sort articles by category, then alphabetically
const sortedArticles = allArticles.sort((a, b) => {
  const categoryComparison = a.data.category.localeCompare(b.data.category);
  if (categoryComparison !== 0) return categoryComparison;
  return a.data.title.localeCompare(b.data.title);
});

// Group articles by category
const articlesByCategory = sortedArticles.reduce((acc, article) => {
  const category = article.data.category;
  if (!acc[category]) {
    acc[category] = [];
  }
  acc[category].push(article);
  return acc;
}, {});

// Get categories and sort them, with Editor's Note first
const categories = Object.keys(articlesByCategory).sort();
if (categories.includes("Editor's Note")) {
  categories.splice(categories.indexOf("Editor's Note"), 1);
  categories.unshift("Editor's Note");
}

// Generate excerpts for all articles
const articlesWithExcerpts = {};
for (const category of categories) {
  articlesWithExcerpts[category] = await Promise.all(
    articlesByCategory[category].map(async (article) => {
      const { body } = article;
      const excerpt = generateExcerpt(body, 250);
      return { ...article, excerpt };
    })
  );
}
---

<div class="magazine-content">
  <div class="magazine-container">
    <div class="magazine-header">
      <h1 class="issue-title">Volume 1</h1>
      <time class="issue-date">5 May 2025</time>
    </div>

    <div class="articles-container">
      {categories.map((category) => (
        <div class="category-section">
          <div class="category-header">
            <div class="category-divider"></div>
            <span class="category-tag">{category}</span>
          </div>
          
          <div class="articles-list">
            {articlesWithExcerpts[category].map((article) => (
              <article class="article-preview">
                <h3 class="article-title">
                  <a href={`/articles/${article.slug}`} set:html={formatTitle(article.data.title)}>
                  </a>
                </h3>
                <p class="article-author">{article.data.author}</p>
                <p class="article-excerpt">
                  {article.excerpt}
                </p>
              </article>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</div>
<style>
  .magazine-content {
    background: var(--light);
    color: var(--dark);
    padding: 2rem 0;
    min-height: 100vh;
  }

  .magazine-container {
    max-width: 90vw;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .magazine-header {
    text-align: center;
    margin-bottom: 4rem;
    padding-top: 3rem;
  }

  .issue-title {
    font-family: 'Libre Baskerville', serif;
    font-size: 5rem;
    font-weight: 400;
    margin: 0 0 1rem 0;
    line-height: 1;
  }

  .issue-date {
    font-family: 'Libre Baskerville', serif;
    font-style: italic;
    font-size: 1.25rem;
    color: var(--dark);
  }

  .articles-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .category-section {
    margin-bottom: 4rem;
  }

  .category-header {
    margin-bottom: 2rem;
    position: relative;
  }

  .category-divider {
    width: 100%;
    height: 3px;
    background: var(--accent);
    margin-bottom: 1rem;
  }

  .category-tag {
    font-family: 'Alata', sans-serif;
    font-size: 1.25rem;
    font-weight: 400;
    color: var(--accent);
    display: inline-block;
  }

  .articles-list {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    margin-top: 2rem;
  }

  @media (min-width: 768px) {
    .articles-list {
      grid-template-columns: 1fr 1fr;
      gap: 4rem 3rem;
    }
  }

  .article-preview {
    max-width: 600px;
  }

  .article-title {
    font-family: 'Libre Baskerville', serif;
    font-size: 2rem;
    font-weight: 400;
    line-height: 1.2;
    margin: 0 0 0.5rem 0;
  }

  .article-title {
    position: relative;
  }

  .article-title a {
    color: var(--dark);
    text-decoration: none;
    transition: all 0.3s ease;
    display: block;
    line-height: inherit;
  }

  .article-title a::after {
    content: '→';
    opacity: 0;
    transform: translateX(-10px);
    transition: all 0.3s ease;
    color: var(--accent);
    font-size: 0.8em;
  }

  .article-title a:hover {
    color: var(--accent);
    transform: translateX(8px);
  }

  .article-title a:hover::after {
    opacity: 1;
    transform: translateX(0);
  }


  .article-author {
    font-family: 'Libre Baskerville', serif;
    font-style: italic;
    font-size: 1rem;
    color: var(--dark);
    margin: 0 0 1rem 0;
  }

  .article-excerpt {
    font-family: 'Libre Baskerville', serif;
    font-size: 1rem;
    line-height: 1.5;
    color: var(--dark);
    margin: 0;
    text-align: justify;
  }

  @media (max-width: 80rem) {
    .magazine-container {
      padding: 0 1rem;
    }
  }

  @media (max-width: 768px) {
    .magazine-container {
      padding: 0 1rem;
    }
  }

  @media (max-width: 480px) {
    .magazine-container {
      padding: 0 0.75rem;
    }
  }

  @media (max-width: 768px) {
    .issue-title {
      font-size: 3rem;
    }

    .article-title {
      font-size: 2rem;
    }
  }
</style> 
