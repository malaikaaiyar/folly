---
import Layout from "../../layouts/Layout.astro";
import { getCollection } from "astro:content";
import { formatTitle } from "../../utils.js";

// Generate static paths for all published articles
export async function getStaticPaths() {
  const articlesEntries = await getCollection("articles");
  
  return articlesEntries
    .filter(article => article.data.published)
    .map(article => ({
      params: { slug: article.slug },
      props: { article },
    }));
}

// Get the article data from props
const { article } = Astro.props;

// Render the article content
const { Content } = await article.render();

// Format the title
const formattedTitle = formatTitle(article.data.title);
---

<Layout title={article.data.title} desc="">
  <div class="post-page">
    <div class="post-container">
      <div class="post-header">
        <h1 class="post-title" set:html={formattedTitle}></h1>
        <div class="post-meta">
          <div class="post-divider"></div>
          {article.data.author && <p class="post-author">{article.data.author}</p>}
          <time class="post-date">5 May 2025</time>
        </div>
      </div>
      
      <div class={`post-content-wrapper ${article.data.image ? 'has-image' : 'no-image'}`}>
        <div class="post-content">
          <Content />
        </div>
        {article.data.image && (
          <div class="post-image">
            <img src={article.data.image} alt="Article illustration" />
            {article.data.imageCaption && (
              <p class="post-image-caption">{article.data.imageCaption}</p>
            )}
          </div>
        )}
      </div>
    </div>
  </div>
</Layout>
<style>
  .post-page {
    background: var(--light);
    min-height: 100vh;
    padding: 2rem 0;
  }

  .post-container {
    max-width: 70vw;
    margin: 0 auto;
    padding: 0 3rem;
  }

  .post-header {
    margin-bottom: 3rem;
  }

  .post-title {
    font-family: 'Libre Baskerville', serif;
    font-size: 5rem;
    font-weight: 400;
    color: var(--dark);
    margin: 0 0 2rem 0;
    line-height: 1;
    /* max-width: 879px; */
  }

  .post-meta {
    position: relative;
    margin-bottom: 2rem;
  }

  .post-divider {
    width: 100%;
    height: 3px;
    background: var(--accent);
    margin-bottom: 1rem;
  }

  .post-author {
    font-family: 'Libre Baskerville', serif;
    font-style: italic;
    font-size: 1rem;
    color: var(--accent);
    margin: 0;
    max-width: 430px;
  }

  .post-date {
    font-family: 'Libre Baskerville', serif;
    font-style: italic;
    font-size: 1.25rem;
    color: var(--accent);
    position: absolute;
    right: 0;
    top: 1rem;
  }

  .post-content-wrapper.has-image {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .post-content-wrapper.no-image {
    display: block;
  }

  .post-content {
    font-family: 'Libre Baskerville', serif;
    font-size: 1rem;
    line-height: 1.875;
    color: var(--dark);
    text-align: justify;
  }

  .post-content :global(p) {
    margin-bottom: 1.875rem;
  }

  .post-content :global(p:last-child) {
    margin-bottom: 0;
  }

  .post-content :global(h2) {
    font-family: 'Libre Baskerville', serif;
    font-size: 1.5rem;
    font-weight: 400;
    margin: 2.5rem 0 1.5rem 0;
    line-height: 1.2;
  }

  .post-content :global(h3) {
    font-family: 'Libre Baskerville', serif;
    font-size: 1.25rem;
    font-weight: 400;
    margin: 2rem 0 1rem 0;
    line-height: 1.3;
  }

  .post-content :global(blockquote) {
    border-left: 3px solid var(--accent);
    padding-left: 1.5rem;
    margin: 2rem 0;
    font-style: italic;
    color: var(--dark);
  }

  .post-content :global(strong) {
    font-weight: 700;
  }

  .post-content :global(em) {
    font-style: italic;
  }

  .post-image {
    position: sticky;
    top: 8rem;
  }

  .post-image img {
    width: 100%;
    height: auto;
    max-width: 599px;
    border-radius: 0;
  }

  .post-image-caption {
    font-family: 'Libre Baskerville', serif;
    font-size: 0.875rem;
    font-style: italic;
    color: var(--primary);
    margin: 0.75rem 0 0 0;
    text-align: right;
    line-height: 1.4;
  }

  @media (max-width: 80rem) {
    .post-container {
      padding: 0 1rem;
    }
  }

  @media (max-width: 1200px) {
    .post-content-wrapper.has-image {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    
    .post-content-wrapper.has-image .post-image {
      position: static;
      order: -1;
    }
  }

  @media (max-width: 768px) {
    .post-container {
      max-width: 95vw;
      padding: 0 1rem;
    }
    
    .post-title {
      font-size: 2.5rem;
      line-height: 1.1;
      margin-bottom: 1.5rem;
    }
    
    .post-meta {
      margin-bottom: 2rem;
    }
    
    .post-divider {
      margin-bottom: 0.75rem;
    }
    
    .post-meta {
      display: block;
    }
    
    .post-author {
      font-size: 0.9rem;
      margin-bottom: 0;
      display: inline-block;
    }
    
    .post-date {
      position: static;
      margin-top: 0;
      font-size: 1rem;
      float: right;
    }
    
    .post-content-wrapper.has-image {
      gap: 2rem;
    }
    
    .post-content {
      font-size: 0.95rem;
      line-height: 1.7;
    }
    
    .post-content :global(p) {
      margin-bottom: 1.5rem;
    }
    
    .post-content :global(h2) {
      font-size: 1.25rem;
      margin: 2rem 0 1rem 0;
    }
    
    .post-content :global(h3) {
      font-size: 1.1rem;
      margin: 1.5rem 0 0.75rem 0;
    }
  }

  @media (max-width: 480px) {
    .post-page {
      padding: 1rem 0;
    }
    
    .post-container {
      max-width: 100vw;
      padding: 0 0.75rem;
    }
    
    .post-title {
      font-size: 2rem;
      line-height: 1.2;
      margin-bottom: 1rem;
    }
    
    .post-header {
      margin-bottom: 2rem;
    }
    
    .post-meta {
      margin-bottom: 1.5rem;
    }
    
    .post-author {
      font-size: 0.85rem;
    }
    
    .post-date {
      font-size: 0.9rem;
    }
    
    .post-content {
      font-size: 0.9rem;
      line-height: 1.6;
    }
    
    .post-content :global(p) {
      margin-bottom: 1.25rem;
    }
    
    .post-content :global(h2) {
      font-size: 1.15rem;
      margin: 1.5rem 0 0.75rem 0;
    }
    
    .post-content :global(h3) {
      font-size: 1rem;
      margin: 1.25rem 0 0.5rem 0;
    }
    
    .post-content :global(blockquote) {
      padding-left: 1rem;
      margin: 1.5rem 0;
    }
  }
</style> 
