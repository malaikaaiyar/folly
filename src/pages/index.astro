---
import { site } from "../site"
import Layout from "../layouts/Layout.astro"
import MagazineSection from "../components/MagazineSection.astro"
---

<Layout title={site.pages.index.title} desc={site.pages.index.description} hideFooter={true}>
  <!-- Hero Section -->
  <section class="hero-section">
    <div class="hero-content">
      <div class="hero-text">
        <h1 class="hero-title">FOLLY <br> MAGAZINE</h1>
        <p class="hero-subtitle">
          An Oxford student magazine publishing long-form creative work in literature, politics, and the arts.
        </p>
        <br>
        <p class="hero-subtitle"><a class="volume-link" href="/magazine"><em>Go to Volume 2</em></a></p>
      </div>
      <div class="hero-graphics">
        <!-- Decorative graphics placeholder -->
        <img src="/images/fin.png" alt="Oxford Illustration" class="hero-illustration" />
      </div>

    </div>
  </section>

  <!-- Rotating Banner -->
  <!-- <section class="rotating-banner">
    <div class="banner-wrapper">
      <div class="banner-content">
        <span class="banner-text">volume 2 coming jan 2026</span>
        <span class="banner-text">volume 2 coming jan 2026</span>
        <span class="banner-text">volume 2 coming jan 2026</span>
        <span class="banner-text">volume 2 coming jan 2026</span>
        <span class="banner-text">volume 2 coming jan 2026</span>
        <span class="banner-text">volume 2 coming jan 2026</span>
      </div>
      <div class="banner-content" aria-hidden="true">
        <span class="banner-text">volume 2 coming jan 2026</span>
        <span class="banner-text">volume 2 coming jan 2026</span>
        <span class="banner-text">volume 2 coming jan 2026</span>
        <span class="banner-text">volume 2 coming jan 2026</span>
        <span class="banner-text">volume 2 coming jan 2026</span>
        <span class="banner-text">volume 2 coming jan 2026</span>
      </div>
    </div>
  </section> -->

  <!-- Magazine Content Section -->
  <!-- <MagazineSection /> -->
</Layout>

<style>
  /* Reset for this page */
  :global(body) {
    margin: 0;
    padding: 0;
    padding-top: 0; /* Remove the fixed nav padding for full-height hero */
    max-width: none;
  }
  
  @media (max-width: 80rem) {
    :global(body) {
      padding: 0; /* Override the responsive padding for index */
    }
  }
  
  :global(main) {
    gap: 0;
  }



  /* Hero Section */
  .hero-section {
    background: var(--light);
    color: var(--dark);
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    position: relative;
    overflow: hidden;
  }



  .hero-content {
    flex: 1;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
    padding: 6rem 3rem 3rem 3rem;
    max-width: 70rem;
    margin: 0 auto;
    width: 100%;
  }

  .hero-text {
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .hero-graphics {
    display: flex;
    justify-content: center;
    align-items: center;
    opacity: 1;
  }

  .hero-illustration {
    max-width: 100%;
    width: 100%;
    height: auto;
  }

  .hero-title {
    font-family: 'Libre Baskerville', serif;
    font-size: clamp(3rem, 5.5vw, 6rem);
    font-weight: 400;
    line-height: 1;
    margin: 0 0 1rem 0 !important;
    letter-spacing: -0.02em;
  }

  .hero-subtitle {
    font-family: 'Alata', sans-serif;
    /* font-style: italic; */
    font-size: 1.25rem;
    line-height: 1.4;
    max-width: 600px;
    margin: 0;
  }

  /* Volume 2 link */
  .volume-link-wrapper {
    grid-column: 1 / -1;
    display: flex;
    justify-content: center;
    margin-top: 2rem;
  }

  .volume-link {
    font-size: 1.25rem;
    text-decoration: none;
    color: var(--dark);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    position: relative;
    transition: all 0.3s ease;
  }

  .volume-link::after {
    content: '→';
    opacity: 0;
    transform: translateX(-8px);
    transition: all 0.3s ease;
    color: var(--accent);
    font-size: 0.9em;
  }

  .volume-link:hover {
    color: var(--accent);
    transform: translateX(6px);
  }

  .volume-link:hover::after {
    opacity: 1;
    transform: translateX(0);
  }

  /* Rotating Banner */
  .rotating-banner {
    background: var(--dark);
    color: var(--light);
    padding: 1rem 0;
    overflow: hidden;
    white-space: nowrap;
    position: relative;
    width: 100%;
    margin-bottom: 1rem;
  }

  .banner-wrapper {
    display: flex;
    width: fit-content;
    animation: scroll 20s linear infinite;
    will-change: transform;
  }

  .banner-content {
    display: inline-flex;
    gap: 4rem;
    white-space: nowrap;
    flex-shrink: 0;
    margin-right: 4rem;
  }

  .banner-text {
    font-family: 'Alata', sans-serif;
    font-size: 1.2rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    display: inline-block;
    white-space: nowrap;
    flex-shrink: 0;
  }

  @keyframes scroll {
    from {
      transform: translate3d(0, 0, 0);
    }
    to {
      transform: translate3d(-50%, 0, 0);
    }
  }

  @media (max-width: 768px) {
    .rotating-banner {
      display: none;
    }
  }

  /* Magazine Section */
  .magazine-section {
    background: var(--light);
    color: var(--dark);
    padding: 2rem 0;
    min-height: 100vh;
  }

  .magazine-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .issue-title {
    font-family: 'Libre Baskerville', serif;
    font-size: 5rem;
    font-weight: 400;
    margin: 0 0 1rem 0;
  }

  .issue-date {
    font-family: 'Libre Baskerville', serif;
    font-style: italic;
    font-size: 1.25rem;
    color: var(--dark);
  }

  .articles-container {
    max-width: 70rem;
    margin: 0 auto;
    padding: 0 3rem;
  }

  .category-section {
    margin-bottom: 4rem;
  }

  .category-header {
    margin-bottom: 2rem;
    position: relative;
  }

  .category-header::after {
    content: '';
    position: absolute;
    left: 0;
    top: -3px;
    width: 90vw;
    height: 3px;
    background: var(--accent);
  }

  .category-tag {
    background: var(--accent);
    color: var(--light);
    padding: 0.75rem 2rem;
    font-family: 'Alata', sans-serif;
    font-size: 1.25rem;
    font-weight: 500;
    display: inline-block;
    position: relative;
  }

  .articles-list {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    margin-top: 2rem;
  }

  @media (min-width: 768px) {
    .articles-list {
      grid-template-columns: 1fr 1fr;
      gap: 4rem 3rem;
    }
  }

  .article-preview {
    max-width: 600px;
  }

  .article-title {
    font-family: 'Libre Baskerville', serif;
    font-size: 2.5rem;
    font-weight: 400;
    line-height: 1.1;
    margin: 0 0 0.5rem 0;
  }

  .article-title a {
    color: var(--dark);
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .article-title a:hover {
    color: var(--accent);
  }

  .article-author {
    font-family: 'Libre Baskerville', serif;
    font-style: italic;
    font-size: 1rem;
    color: var(--dark);
    margin: 0 0 1rem 0;
  }

  .article-excerpt {
    font-family: 'Libre Baskerville', serif;
    font-size: 1rem;
    line-height: 1.5;
    color: var(--dark);
    margin: 0;
    text-align: justify;
  }

  /* Smooth scrolling */
  html {
    scroll-behavior: smooth;
  }

  /* Hero Animations - Desktop Only */
  @media (min-width: 769px) {
    @keyframes fadeInLeft {
      from {
        opacity: 0;
        transform: translateX(-50px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    @keyframes fadeInRight {
      from {
        opacity: 0;
        transform: translateX(50px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    @keyframes scaleIn {
      from {
        opacity: 0;
        transform: scale(0.9);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }
  }

  /* Disable animations on mobile for performance */
  @media (max-width: 768px) {
    .hero-text,
    .hero-graphics,
    .hero-illustration {
      animation: none;
    }
  }


  /* Responsive adjustments */
  @media (max-width: 768px) {
    .hero-content {
      grid-template-columns: 1fr;
      padding: 4rem 1rem 3rem 1rem;
    }

    .hero-graphics {
      order: -1;
    }

    .hero-title {
      font-size: clamp(2.5rem, 6vw, 4rem);
    }

    .articles-container {
      padding: 0 1rem;
    }

    .issue-title {
      font-size: 3rem;
    }

    .article-title {
      font-size: 2rem;
    }
  }
</style>
